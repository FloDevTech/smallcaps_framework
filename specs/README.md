# Especificaciones históricas

Esta carpeta contiene las especificaciones de funciones ya implementadas y cerradas,
conservadas como evidencia histórica. No se modifican ni se reimplementan.

El flujo SDD activo usa `openspec/specs/` y `openspec/changes/`.
